package com.kobe.warehouse.domain.enumeration;

public enum OrigineGeneration {
    /** Facture créée manuellement depuis l'interface utilisateur. */
    MANUELLE,
    /** Facture générée automatiquement par le planificateur. */
    AUTO
}
