<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera permission for barcode scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
      La caméra est un confort et non une nécessité : la saisie manuelle et les
      douchettes HID permettent de compter sans elle. `required="false"` évite d'exclure
      les terminaux d'inventaire et tablettes dépourvus de caméra.
    -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- Storage for offline data -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Vibrate permission for scanner feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".PharmaSmartInventoryApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PharmaSmartInventory"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Splash Activity -->
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.activity.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!--
          Aucune activité ne fixe son orientation : l'affichage suit le réglage
          « rotation automatique » du terminal, comportement Android par défaut.
        -->

        <!-- Inventory List Activity -->
        <activity
            android:name=".ui.activity.InventoryListActivity"
            android:exported="false" />

        <!--
          Inventory Detail Activity (scanning and counting).
          adjustResize : la saisie se fait dans la ligne, la liste doit donc se
          réduire à l'ouverture du clavier pour que la ligne éditée reste visible.
        -->
        <activity
            android:name=".ui.activity.InventoryDetailActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Scan ponctuel plein écran (CameraX + ML Kit) -->
        <activity
            android:name=".scanner.ScanActivity"
            android:exported="false" />

        <!-- WorkManager for background sync -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>
